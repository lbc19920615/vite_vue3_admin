interface ElOption {
    label: string,
    value: any
}

export const options : Array<ElOption> = [
    {
        label: 'sdsds',
        value: 1
    }
]

export type WindowStates = "open" | "closed" | "minimized";

